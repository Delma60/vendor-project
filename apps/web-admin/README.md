# FoodConnect Admin Dashboard

Platform operations for seller approval, users, orders, finance, promotions, moderation, analytics, and permissions.

Run from the repository root with `npm run dev:admin`.
