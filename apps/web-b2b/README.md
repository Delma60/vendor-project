# FoodConnect B2B Portal

Public company-ordering marketing pages and an authenticated portal for bulk orders, approvals, recurring orders, invoices, pricing, and tracking.

Run from the repository root with `npm run dev:b2b`.
